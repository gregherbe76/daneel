4 caveats arbitrés. Va à l'étape 2.

Décision 1 — Vocabulaire : utilise decisionScore comme base (pas 
fitScore brut). Le brief disait "matching_score" comme terme 
générique, dans le code ça correspond à decisionScore.

Décision 2 — Persistance : Option A. Étends le type ShortlistEntry 
dans jsonb summaries avec : matchingScore (= decisionScore), 
codematchOverall, bonusApplied, finalScore, techEvaluated. Aucune 
migration SQL, juste TypeScript + OpenAPI codegen.

Décision 3 — Council reste sur le score brut. NE MODIFIE PAS 
runDecisionStep. evalWithNames continue de recevoir 
ai_evaluations.score non boosté. Council délibère sur la base 
non-boostée. Documente cette décision dans 
docs/providers/codematch.md sous la section "Impact on shortlist 
ranking" : 'Council deliberates on the matching_score base. 
CodeMatch boost is applied to shortlist ranking only, not to 
Council deliberation.'

Décision 4 — Backward compat : zéro backfill. Le rendu UI 
(detail.tsx et report.tsx) doit traiter les nouveaux champs comme 
optionnels :
- Si finalScore === undefined : affiche decisionScore comme score 
  principal (comportement pré-4.3)
- Si techEvaluated !== true : pas de badge "⚡ Tech evaluated"
- Si bonusApplied === undefined : pas de breakdown au tooltip
Les anciens runs continuent de s'afficher comme avant. Les nouveaux 
runs (post-4.3) bénéficient des nouvelles colonnes.

Précisions complémentaires sur l'étape 2 :

A. Logique de ranking — utilise decisionScore comme matching_score 
   dans la formule. Le tri reste sur final_score DESC.

B. Schéma BDD — skip cette section. Option A retenue, pas de 
   migration. Juste l'extension du type ShortlistEntry dans 
   lib/db/src/schema/ai-evaluations.ts (ou wherever ShortlistEntry 
   est typé) + OpenAPI codegen.

C. UI shortlist — utilise le tooltip shadcn (déjà disponible via 
   @radix-ui/react-tooltip + src/components/ui/tooltip.tsx). 
   Pas de tooltip natif HTML.

D. Tests — les 5 tests décrits dans le brief original restent 
   valides, juste avec decisionScore comme entrée au lieu de 
   matching_score.

Va à l'étape 2 maintenant.